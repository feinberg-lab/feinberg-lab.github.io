baseURL: "https://smhakkinen.github.io/website_skeleton/"
title: "My Lab Site"
theme: "github.com/HugoBlox/hugo-blox-builder"

# Path & Cache Settings
canonifyURLs: true
relativeURLs: false
ignoreCache: true

# Theme Specific Parameters (YAML style)
params:
  # This tells Hugo not to use security hashes that can break on GitHub Pages
  fingerprint: false